// Compatibility shim — widgets live in their own folders under ./widgets/*.
// This file preserves the old BasicWidgets import path.
export {
  TimerWidget,
  StopwatchWidget,
  DiceWidget,
  ScoreWidget,
  CalculatorWidget,
} from "./index";
